---
version: 4
datadir: data
hierarchy:
  - name: "LSB Distribution"
    backend: yaml
    path: "lsbdistcodename/%{facts.os.distro.codename}"

  - name: "OS family"
    backend: yaml
    path: "osfamily/%{facts.os.family}"

  - name: "common"
    backend: yaml

